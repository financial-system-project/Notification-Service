server:
  port: 8084

spring:
  application:
    name: notification-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.financial.notificationservice: DEBUG